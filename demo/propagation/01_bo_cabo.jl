# initialize GP on θ-space

for iter in 1:max_iter

    # incumbent
    # θ_star = argmin(μ(θ) + α σ(θ))

    # acquisition
    # EI(θ)

    # optimize EI
    # θ_plus = PSO(EI)

    # expensive model
    # y_plus = G(θ_plus)

    # update GP
    # add sample
    # fit GP

    # convergence
    # if δ_BO < tol
    #    break
end