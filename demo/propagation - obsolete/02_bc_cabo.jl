# given θ_fixed

for iter in 1:max_iter

    # choose next u via PVC
    # u_plus = argmax PVC(u)

    # expensive model evaluation
    # y_plus = G(u_plus, θ_fixed)

    # update GP
    # fit surrogate

    # estimate integral/moments

    # BC convergence
    # if δ_BC < tol
    #    break
end